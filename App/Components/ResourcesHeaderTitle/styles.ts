import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  heading: {
    marginBottom: 5
  },
  subHeading: {
    flexDirection: 'row'
  },
  subHeadingText: {
    marginLeft: 5
  }
});
